import Header from '@/components/header/Header-legacy'
import React from 'react'

const ProjectLayout = ({children}) => {
  return (
    <div className='bg-neutral-100 h-full '>
      
      {children}
    </div>
  )
}

export default ProjectLayout